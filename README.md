# Convex Path Curves

Interactive React+Vite tool to build responsive SVG clip-paths from a grid.

## Usage

```bash
npm install
npm run dev      # dev server on :5173
npm test         # run algorithm tests
npm run build    # production build
```
